# 1. User Identity Verification
whoami
groups $USER
# Explanation: Displayed current username and all groups. The output shows my login ID and associated groups.

# 2. Workspace Validation
pwd
ls -la
# Explanation: Showed current directory and listed all files with details. The long format shows permissions, owners, and timestamps.

# 3. Environment Confirmation File
echo "Linux user environment verified" > user_info.txt
# Explanation: Created a file and wrote the verification text. The file now contains the confirmation message.

# 4. File Integrity Check
wc -m user_info.txt
# Explanation: Counted characters in the file. The output shows 31 characters including the newline.

# 5. Learning the Tools
man mkdir | head -30
# Explanation: Accessed mkdir manual. One useful option is -p which creates parent directories if they don't exist.

# 6. Home Directory Inspection
ls -la ~ | sort
# Explanation: Listed home directory contents alphabetically. This helps organize and find files easily.

# 7. Log Investigation
grep "admin" log.txt
# Explanation: Searched for 'admin' in log.txt. This filters log entries containing administrative activities.

# 8. System Information Check
uname -r
# Explanation: Displayed kernel version. This shows the Linux kernel currently running on the system.

# 9. Network Connectivity Test
ping -c 4 www.google.com
# Explanation: Sent 4 ICMP packets to Google. The output shows network connectivity and response times.

# 10. System Health Awareness
uptime
# Explanation: Showed system uptime. The output displays uptime duration, logged-in users, and load averages for 1, 5, and 15 minutes.
